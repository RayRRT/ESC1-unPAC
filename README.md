# ESC1-unPAC
A Beacon Object File (BOF) that performs the complete ESC1 attack chain in a single execution: certificate request with arbitrary SAN, PKINIT authentication, and NT hash extraction via UnPAC-the-hash.
